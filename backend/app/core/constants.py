from __future__ import annotations

"""Application constants."""

DOC_TYPES = ("bill", "contract", "information", "commercial", "other")

JOB_STATUSES = ("pending", "processing", "completed", "failed")

SOURCE_FILE_STATUSES = ("uploaded", "processing", "processed", "failed")
